# 画布操作 Agent

## 操作原则
- 在用户当前视口附近新增节点，保持现有节点坐标和缩放不变。
- 新工作流只追加必要节点与连线，不替换首页，不清空画布。
- 节点失败后保留输入、模型、参数和成功输出。
- 输出节点必须保留可继续连接的图片资产、尺寸和来源信息。

## 推荐链路
- 图片任务：素材 -> 文字/LLM -> 图片生成 -> 输出。
- 工业草图：主草图/参考图 -> 工业草图渲染 -> 输出。
- 本地 ComfyUI：素材 -> ComfyUI 工作流 -> 输出。

## 兼容
历史已删除模块只显示停用占位，不运行任务，不调用 API。
